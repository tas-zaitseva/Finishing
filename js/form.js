
  let form = document.querySelector('#form');
  
  let priceInput = form.elements.priceInput;
  let priceScale = form.elements.priceScale;

  form.addEventListener('submit', function(event) {
    event.preventDefault();
  })


